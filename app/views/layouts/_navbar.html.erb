<div class="container">
    <header><%= link_to "Superstar Pro Wrestling Game Card Maker", root_path %></header>
    <nav>
        <ul class="nav navbar-nav">
          <li class="active"><a href="/">HOME</a></li>
          <li>
            <div class="dropdown">
                <button class="dropbtn">CREATE A NEW WRESTLER
                  <i class="fa fa-caret-down"></i>
                </button>
                <div class="dropdown-content">
                  <%= link_to "BLANK FORM", new_wrestler_path %>
                  <%= link_to "SET OC AT 13%", wrestler_templates_new_path(oc: "13") %>
                  <%= link_to "SET OC AT 11%", wrestler_templates_new_path(oc: "11") %>
                  <%= link_to "SET OC AT 8%", wrestler_templates_new_path(oc: "8") %>
                  <%= link_to "SET OC AT 6%", wrestler_templates_new_path(oc: "6") %>
                </div>
              </div>
          </li>          
          <li>
            <div class="dropdown">
                <button class="dropbtn">CARDS BY CATEGORY
                  <i class="fa fa-caret-down"></i>
                </button>
                <div class="dropdown-content">
                    <%=link_to "ALL WRESTLERS", wrestlers_path %>
                  <a href="<%= division_path (Division.find_by name: 'Cruiserweight').id %>">Cruiserweights</a>
                  <a href="<%= division_path (Division.find_by name: 'Legend').id %>">Legends</a>
                  <a href="<%= division_path (Division.find_by name: 'Midcard').id %>">Midcarders</a>
                  <a href="<%= division_path (Division.find_by name: 'Preliminary').id %>">Jobbers</a>
                  <a href="<%= division_path (Division.find_by name: 'Regional Champion').id %>">Regional Champions</a>
                  <a href="<%= division_path (Division.find_by name: 'Super Heavyweight').id %>">Super Heavyweights</a>
                  <a href="<%= tag_team_wrestlers_index_path %>">Team Team Experts</a>
                  <a href="<%= division_path (Division.find_by name: 'Veteran').id %>">Veterans</a>
                  <a href="<%= division_path (Division.find_by name: 'Women').id %>">Women</a>
                  <a href="<%= division_path (Division.find_by name: 'World Champion').id %>">World Champions</a>
                  <a href="<%= division_path (Division.find_by name: 'Young Star').id %>">Young Stars</a>
                </div>
              </div>
            </li>
          <li>
            <div class="dropdown">
                <button class="dropbtn">CARDS BY PROMOTION
                  <i class="fa fa-caret-down"></i>
                </button>
                <div class="dropdown-content">
                    <%=link_to "ALL WRESTLERS", wrestlers_path %>
                  <a href="<%= promotion_path (Promotion.find_by name: 'AWA').id %>">AWA</a>
                  <a href="<%= promotion_path (Promotion.find_by name: 'Florida').id %>">Florida</a>
                  <a href="<%= promotion_path (Promotion.find_by name: 'Georgia').id %>">Georgia</a>
                  <a href="<%= promotion_path (Promotion.find_by name: 'Memphis').id %>">Memphis</a>
                  <a href="<%= promotion_path (Promotion.find_by name: 'Mid Atlantic').id %>">Mid Atlantic</a>
                  <a href="<%= promotion_path (Promotion.find_by name: 'Mid South').id %>">Mid South</a>
                  <a href="<%= promotion_path (Promotion.find_by name: 'NWA').id %>">NWA</a>
                  <a href="<%= promotion_path (Promotion.find_by name: 'Southwest').id %>">Southwest</a>
                  <a href="<%= promotion_path (Promotion.find_by name: 'World Class').id %>">World Class</a>
                  <a href="<%= promotion_path (Promotion.find_by name: 'Women').id %>">Women</a>
                  <a href="<%= promotion_path (Promotion.find_by name: 'WWC').id %>">WWC</a>
                  <a href="<%= promotion_path (Promotion.find_by name: 'WWF').id %>">WWF</a>
                </div>
              </div>
            </li>
          <li><%=link_to "ABOUT", welcome_about_path %></li>
        </ul>
  </nav>
</div>